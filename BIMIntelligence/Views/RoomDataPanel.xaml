<Window x:Class="BIMIntelligence.Views.RoomDataPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Data Extractor"
        Width="800" Height="500"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Room Data Extractor"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="#1a1a1a"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
                   Text="Extracted room data from the current Revit model"
                   FontSize="12"
                   Foreground="#666666"
                   Margin="0,0,0,12"/>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2"
                  x:Name="RoomDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  CanUserResizeColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderBrush="#e0e0e0"
                  BorderThickness="1"
                  RowHeight="32"
                  ColumnHeaderHeight="36"
                  Background="White"
                  AlternatingRowBackground="#f9f9f9">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#f5f5f5"/>
                    <Setter Property="Foreground" Value="#333333"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="BorderBrush" Value="#e0e0e0"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Room Name" Binding="{Binding RoomName}" Width="*" />
                <DataGridTextColumn Header="Number" Binding="{Binding RoomNumber}" Width="80" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100" />
                <DataGridTextColumn Header="Area (m²)" Binding="{Binding AreaSqM, StringFormat=N2}" Width="90" />
                <DataGridTextColumn Header="Doors" Binding="{Binding DoorCount}" Width="60" />
                <DataGridTextColumn Header="Windows" Binding="{Binding WindowCount}" Width="70" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       x:Name="StatusText"
                       VerticalAlignment="Center"
                       Foreground="#666666"
                       FontSize="12"/>

            <Button Grid.Column="2"
                    Content="Export to JSON"
                    Click="OnExportClick"
                    Padding="16,8"
                    FontSize="13"
                    Background="#2563EB"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
